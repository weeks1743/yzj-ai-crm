# 重构总览与产品边界

## 本篇回答什么问题

本篇回答以下问题：

- `AI销售助手` 到底是什么产品
- 为什么要在 `yzj-ai-crm` 中重做，而不是延续旧实现
- v1 的业务边界、系统边界、能力边界分别是什么
- 哪些内容纳入本次范围，哪些留到后续

## 产品定义

`AI销售助手` 是一个以对话为主入口的销售智能助手系统。

它不是传统 CRM 页面系统的 AI 插件，也不是轻云应用的侧边增强，而是一个以 `AI 可理解、可调用、可编排` 为前提重新设计的销售助手。用户主要通过 AI 对话完成工作，轻云记录系统承担结构化主数据记录与查询职责。

产品对外统一命名为：

- 中文名：`AI销售助手`

轻云中的应用实例命名建议为：

- `AI销售助手_记录系统`

## 为什么要在 yzj-ai-crm 重新开发

本项目默认忽略现有实现，按“重新开发一个 AI-CRM”处理，原因如下：

1. 旧系统的路由、数据层和能力层约束会影响新设计收口
2. 本次目标不是局部修补，而是建立一套面向未来助手产品可复用的底层结构
3. 你当前已经明确系统分层、租户隔离、影子系统策略和核心场景，应直接围绕未来形态设计

因此，本次文档和后续开发都以：

- 新的产品边界
- 新的系统分层
- 新的数据模型
- 新的技能体系

作为出发点，而不是围绕旧项目代码做迁移适配。

## 三层逻辑分层

`AI销售助手` 内部采用三层逻辑分层：

### 1. 系统基础设置层

负责所有系统级配置和租户级配置，包括：

- EID
- APPID
- 密钥与回调地址
- OAuth 接入
- 身份解析
- 组织同步
- 大模型配置
- 文件存储与转写配置
- 外部研究配置

### 2. 影子系统层

基于云之家轻云搭建的 `AI销售助手_记录系统`，负责承载结构化主数据。

v1 只要求打通以下对象的操作：

- 客户
- 联系人
- 商机
- 商机跟进记录

### 3. AI-CRM 层

负责真正的 AI 能力，包括：

- 主 Agent 路由与编排
- 记录系统技能动态生成
- 场景技能中心
- 录音导入与分析
- 公司分析与企业研究
- 准备拜访材料
- 非结构化数据存储与消费
- 任务系统、对话层、可观测性

## v1 核心范围

### 结构化对象

v1 核心对象固定为：

- 客户
- 联系人
- 商机
- 商机跟进记录

### 核心场景能力

v1 的核心场景能力固定为：

- 录音导入与拜访分析
- 公司分析与企业研究
- 准备拜访材料

### 对话层能力

v1 对话层必须支持：

- 自然语言触发记录系统技能
- 参数缺失时的结构化澄清
- 查询和分析
- 后台任务
- 写操作确认
- 历史任务恢复

## 本次不纳入范围

以下能力不纳入 v1 正式范围：

- 销售订单、销售报价单、订单变更的 AI 技能开放
- 合同助手
- 数字人营销
- 自动日程推送
- 多 Agent 层级组织
- PPT 作为标准交付产物
- 图数据库

这些能力可以在文档中保留扩展位，但不作为本轮设计落地约束。

## 用户与价值

### 目标用户

主要用户是 B2B 销售人员与销售管理者。

用户特征：

- 非技术人员
- 说话口语化、上下文不完整
- 需要移动端 / 轻应用环境使用
- 工作节奏被打断频繁
- 对录入效率、跟进质量、拜访准备质量敏感

### 产品价值

相对传统 CRM，本产品要解决的不是“页面填表更快”，而是：

- 让销售少切页面
- 让高频录入和查询对话化
- 让音频、网页、研究、历史跟进等非结构化内容进入业务主流程
- 让准备拜访材料、跟进分析、公司研究从人工拼凑变成系统能力

## v1 目标结果

如果 v1 成功，应达到以下结果：

1. 用户可以通过 AI 对话完成客户、联系人、商机、商机跟进记录的高频录入与查询
2. 用户可以导入录音，生成可确认写回的跟进记录候选
3. 用户可以分析目标公司，并把研究结果沉淀为可复用资产
4. 用户可以基于影子系统数据、公司分析和历史录音分析生成拜访简报
5. 系统具备 EID + APPID 强隔离、任务可追踪、AI 可观测、场景可扩展能力

## 本篇结论

本项目不是“给轻云 CRM 加一个 AI 小助手”，而是“以 AI销售助手为主产品、轻云记录系统为影子主数据底座”的新系统。

本次重构的核心不是页面，而是：

- 对话入口
- 技能体系
- 场景能力
- 数据资产
- 租户隔离
- 可观测性

这也是后续全部文档的共同前提。
