## 轻应用开发

点击此处查看[视频教程](https://vip.kingdee.com/school/229925845957980672)

#### 什么是轻应用

轻应用是运行在云之家客户端或Web门户中的`网页程序`。

`平台级轻应用`是云之家提供的通用应用，包括智能审批、智能考勤、时间助手、智能会议等。它们可以开箱即用，也可以与企业已有业务系统进行对接。

`企业自建应用`是企业针对自身业务创建的，它与平台级应用一样，也可以在云之家客户端中运行并利用云之家客户端能力和用户身份。

#### 轻应用如何运行

![轻应用开发概述](/opendocs/file/image/45e26c7df023202a341a303e686a55f0)

名词解释：

```
云之家WebView： 是一个标准浏览器组件，为网页代码提供运行环境。

云之家客户端能力： 包括客户端方法、界面和事件。这些能力只能通过JSSDK（qing.js）来访问。

云之家基础接口： 包括鉴权、获取人员组织等基础接口，以及智能审批等业务接口。
```

从上面的图片可以看出，开发轻应用与开发普通网页最主要的区别就是可以 [调用客户端的能力](/tutorial/index?id=jssdk) 和 [获取用户身份](/tutorial/index?id=获取用户身份)， 因此，开发阶段请多参考这两个文档。

<!-- ## 轻应用的开发流程


```dot
digraph step{

  shape0 [shape=box, label="创建应用", width=2]
  shape1 [shape=box, label="注册应用", width=2]
  shape2 [shape=box, label="部署上线", width=2]
  shape3 [shape=box, label="配置轻应用地址", width=2]
  shape4 [shape=box, label="上线", width=2]
  shape0 -> shape1 [label="", fontsize=8]
  shape1 -> shape2 [label="", fontsize=8]
  shape2 -> shape3 [label="", fontsize=8]
  shape3 -> shape4 [label="", fontsize=8]
  
}

``` -->

#### 如何开发轻应用

首先需要在 `管理中心-轻应用管理` 中注册一个新的轻应用，并妥善保管 `appSecret`， 这个 `appSecret` 对于从ticket解析用户身份至关重要。

注册好轻应用之后，你就可以创建你自己的网页程序并部署到自己的服务器上了。（不要忘记返回管理中心填写轻应用的访问地址）

此时你的轻应用还在开发调试阶段，你大概不想除了开发者以外的其它员工无意中访问到它，所以你可以在轻应用管理页面的`可见范围`中设置只对指定人员可见，等到调试完全OK之后再改成对全员可见。


# 创建应用

**注意**：  
1. 只有应用管理员可以进行应用相关的操作   
2. 应用管理员由系统管理员设置，默认系统管理员为应用管理员

#### 1.1 登录管理中心

您需要以应用管理员或者系统管理员的账号登录<a href="https://www.yunzhijia.com" target="_blank">WEB端首页</a>，登录成功后进入管理中心—>轻应用管理。

![](/opendocs/file/image/39610b6659f03b3e513bde7ea187ad35)

#### 1.2 新建轻应用

在轻应用管理中，点击右上角的**新建应用**按钮，可新建轻应用。   
此页面中，系统管理员可通过点击**应用管理员设置**按钮对应用管理员进行设置。

![](/opendocs/file/image/8a4b9e0be4e9633734ae71b1639aa2e5)

点击**新建应用**后，在弹出窗口填入相关对信息。

![](/opendocs/file/image/d025a931837eeb2a2ba7864f5eb97853)

轻应用访问地址应符合URL规范：
![](/opendocs/file/image/4d9a27f1469f4d31a76ee92d49b5b01b)

#### 1.3 获得appid和appsecret

新建轻应用完成保存后，可查看到轻应用信息包含`appid`，`appsecret`，点击右上角“编辑”按钮可以编辑轻应用信息。

![](/opendocs/file/image/c017b17055b756c2297d7138fc4d2a93)

**补充：信息字段填写说明**

字段                                 | 是否必填 | 描述
-------------------------------------|----------|----------------------------------------------------------
应用logo                             | 是       | 此处为默认应用logo，可替换；建议上传图标尺寸为186*186px、大小不超过30K
应用名称                             | 是       | 中文名称：8个汉字以内；英文名称：12个英文字母以内
appId                                | 是       | 在轻应用建好之后自动生成
appSecret                            | 是       | 在轻应用建好之后自动生成
移动端访问地址                       | 是       | 移动端访问入口，还没有访问地址,可点击"点击这里使用轻应用Demo访问地址"，要求为https类的url
Web端访问地址                        | 否       | Web端访问入口，要求为https类的url
桌面端访问地址                       | 否       | 桌面端访问入口，要求为https类的url
打开方式-弹窗形式                    | 否       | 弹出一个H5的页面
打开方式-嵌入形式                    | 否       | 在网页版云之家的框架内嵌入应用页面
打开方式-新建窗口形式                | 否       | 新打开一个web浏览器页面
可见范围                             | 是       | 可见范围，可以按部门，角色，或者个人开通，管理员，全员
是否将应用添加到可见范围人员个人面板 | 否       | 默认勾选


# 获取用户身份

获取用户身份的场景有两种：

一、`服务端获取用户身份（单点登录）`。

二、`客户端获取用户信息（不可作为登录凭证，仅在云之家Webview环境有效，不支持系统浏览器）`。

客户端存有当前登录用户的身份信息，页面通过jssdk调用 `getPersonInfo` 桥可以直接获取。

但请不要把用户身份信息以明文形式在网络上进行传输。如果要把用户身份传递给后端接口，请使用ticket。







#### 服务端获取用户身份
使用场景：实现轻应用应用单点登录（SSO）、获取用户基本信息。

首先，请阅读下面这张流程图。

![](/opendocs/file/image/7b94e47e753642d4b6dfa9dd90ecce6d)

简单来说：

1. `appId` + `secret` = `accessToken`

2. `ticket` + `accessToken` = `用户身份信息`

##### 一、获取accessToken

参考 <a href="docs.html#/server-api/auth/oauth" target="_blank">获取accessToken</a>

##### 二、解析ticket

参考 <a href="docs.html#/server-api/auth/index" target="_blank">解析ticket获取用户身份</a>

用户身份结果示例：

```json
{
    "errorCode": 0,
    "data": {
        "appid": "500041933",
        "xtid": "5a41b292e4b058cf3d0cf312",
        "oid": "5a41b292e4b058cf3d0cf314",
        "eid": "10603457",
        "username": "周小迪",
        "userid": "5a41af0ae4b0820e92cd9868",
        "uid": "101341872",
        "tid": "10603457",
        "jobNo":"12312",
        "networkid": "58df7e16e4b053f44063edca",
        "deviceId": "15900000000",
        "openid": "5a41b292e4b058cf3d0cf314",
        "ticket": null
    },
    "success": true,
    "error": null
}
```

#### 客户端获取用户身份
使用场景：前端仅需显示当前用户信息，非强安全的用户身份识别

通过客户端获取用户身份，要求页面必须引入jssdk，点击右侧链接参考 [jssdk](/tutorial/index/lightapp?id=jssdk) 的引入和初始化的方法。

然后通过执行以下js代码就可以获取用户身份信息了：

```js
qing.call('getPersonInfo', {
  success: function (e) {
    console.info(e) // 打印用户身份信息
  }
})
```
# JSSDK

`qing.js` 是轻应用或卡片和客户端之间的桥梁。

通过它，轻应用或者卡片可以调用客户端的函数、唤起客户端的界面，或者监听客户端的状态变化。

#### 引入JS桥

引入在线版本的qing.js，它将会保持最新

```html
<script src="https://static.yunzhijia.com/public/js/qing/latest/qing.js"></script>
```
注意：你不能直接把上面的js保存到本地使用，因为它只是一个下载器，它会根据页面所在的端下载不同的实现js。

<!--通过 npm 引入离线版本的qing.js-->


#### 调用客户端API

<a href="docs.html#/client-document/index" target="_blank">点击此处</a>查看全部客户端方法

通过 qing.call(方法名, 参数) 可以调用客户端的接口，例如：

```js
// 获取个人信息
qing.call('getPersonInfo', {
    success : function(res){
        alert(JSON.stringify(res))
    }
});
```

上面例子中的 getPersonInfo 是客户端提供的方法之一，它可以获取登录当前客户端的用户的身份信息。

每个客户端接口除了自身必须的参数外，还支持以下**通用参数**：

`success`：接口调用成功时执行的回调函数。

`error`：接口调用失败时执行的回调函数。

`complete`：接口调用完成时执行的回调函数，无论成功或失败都会执行。


以上所述的回调函数的参数说明，如 success : function (res) { ... }，这里的 res 为对象，除了每个接口自身返回的数据之外，还有两个通用属性 code 和 errMsg，其值格式如下：

成功时返回结果示例：`{code: 200, errMsg: "", data: {}}`

失败时返回结果示例：`{code: 500, errMsg: "不合法的图片文件大小", data: {}}`


#### 监听客户端事件

通过 qing.on(事件名, 回调) 可以监听客户端的事件，例如：

```js
qing.on('appear', function () {
    alert('回到当前页面窗口')
})
```

目前支持的事件：

`appear`: 回到当前页面窗口

`disappear`: 离开当前页面窗口

`networkAvailable`: 网络连接变为可用

`networkDisAvailable`: 网络连接变为不可用

`titleDoubleClick`: 标题栏双击

`keyboardChange`: 键盘状态发生变化

#### UserAgent

> 运行在云之家App中的页面的 `navigator.userAgent` 包含以下字段：

| 参数 | 说明 |
| :--- | :--- |
| deviceId | 设备ID |
| deviceName | 设备名称 |
| clientId | 客户端ID，10200表示iOS端，10201表示Android端，10208表示iPad端 |
| os | 系统版本 |
| brand | 手机品牌 |
| model | 手机型号 |
| lang | 客户端当前语言，zh开头为中文，en开头为英文，目前仅支持中文和英文 |
| fontNum | 客户端当前字体大小，0表示标准字体，-1表示小号字体，1、2、3、4依次表示更大号字体（目前仅iOS端支持） |