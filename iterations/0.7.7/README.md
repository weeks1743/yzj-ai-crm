# 0.7.7 项目 README 当前状态刷新

## 版本目标

- 更新根目录 `README.md`，让项目说明从旧的 `0.4.2` 外部技能阶段同步到当前 `0.7.6` Agent 会话治理与 QA 回归阶段。
- 从产品和技术两个层面说明当前项目进展，便于新成员快速理解项目状态。

## 范围

- 仅更新文档。
- 不修改前端、后端运行逻辑。
- 不调整 package 版本号。

## 关键内容

- 明确项目是“管理员后台 + 用户 AI 工作台 + 后端 Agent Runtime + 外部 Skill Runtime”的正式产品原型。
- 补充当前产品能力：
  - CRM 四类对象的自然语言查询、新增、更新。
  - 写入预览、确认、取消。
  - 上下文承接、集合查询隔离、关系字段候选选择。
  - 运行洞察调试区。
- 补充当前技术进展：
  - 主 Agent 核心业务无关。
  - CRM 能力通过 `crm-agent-pack` 和工具元数据表达。
  - Target Grounding Gate、ContextFrame、集合查询判定、唯一命中记忆沉淀。
  - QA-agent 确定性质检和完整多轮会话回归。
- 更新启动方式、目录说明、常用验证命令和当前未完成项。

## 验收

- 文档已更新：`README.md`。
- 本轮为纯文档改动，未执行构建。

## 下一步

- 后续可继续同步 `docs/` 中仍停留在 0.1.x 口径的早期章节。
