interface:
  display_name: "商机删除（影子）"
  short_description: "按formInstIds预演或执行商机批量删除请求"
  default_prompt: "Use $shadow.opportunity_delete to prepare a preview-first 商机 delete request from the current shadow template snapshot."
policy:
  allow_implicit_invocation: false
