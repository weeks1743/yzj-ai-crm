interface:
  display_name: "商机更新（影子）"
  short_description: "按商机模板预演轻云更新请求"
  default_prompt: "Use $shadow.opportunity_update to prepare a preview-first 商机 update request from the current shadow template snapshot."
policy:
  allow_implicit_invocation: false
