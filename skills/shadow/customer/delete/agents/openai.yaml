interface:
  display_name: "客户删除（影子）"
  short_description: "按formInstIds预演或执行客户批量删除请求"
  default_prompt: "Use $shadow.customer_delete to prepare a preview-first 客户 delete request from the current shadow template snapshot."
policy:
  allow_implicit_invocation: false
