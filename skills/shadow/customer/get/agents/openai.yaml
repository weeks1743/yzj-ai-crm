interface:
  display_name: "客户详情读取（影子）"
  short_description: "按formInstId执行或预演客户详情读取请求"
  default_prompt: "Use $shadow.customer_get to prepare a preview-first 客户 get request from the current shadow template snapshot."
policy:
  allow_implicit_invocation: false
