interface:
  display_name: "客户新建（影子）"
  short_description: "按客户模板预演轻云新建请求"
  default_prompt: "Use $shadow.customer_create to prepare a preview-first 客户 create request from the current shadow template snapshot."
policy:
  allow_implicit_invocation: false
