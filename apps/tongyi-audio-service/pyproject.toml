[project]
name = "yzj-tongyi-audio-service"
version = "0.9.0"
description = "Independent Tongyi audio processing service for yzj-ai-crm"
requires-python = ">=3.11"
dependencies = [
  "dashscope>=1.24.1",
  "requests>=2.32.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
